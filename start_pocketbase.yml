---
- name: Start PocketBase Docker container
  hosts: all
  connection: local
  tasks:
    - name: Start PocketBase Docker container
      docker_container:
        name: "{{ instance_code }}"
        state: started
        networks:
          - name: caddy_network
        volumes:
          - "/pocketbase_apps/{{ instance_code }}/pb/migrations:/pb/migrations"
          - "/pocketbase_apps/{{ instance_code }}/pb/pb_data:/pb/pb_data"
